{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/",
            "version": 2,
            "builds": [{ "src": "npm run build", "use": "@vercel/static-build" }]
        }
    ]
}